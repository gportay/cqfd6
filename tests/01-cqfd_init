#!/usr/bin/env bash
#
# validate the behavior of init command

set -o pipefail

. "$(dirname "$0")/jtest.inc" "$1"

cd "$TDIR/" || exit 1

cp .cqfd/docker/Dockerfile .cqfd/docker/Dockerfile.orig

jtest_prepare "run cqfd init with a proper .cqfdrc file should pass"
if cqfd init; then
	jtest_result pass
else
	jtest_result fail
fi

jtest_prepare "run cqfd init with a nonexisting Dockerfile should fail"
cp .cqfdrc .cqfdrc.orig
sed -e "s/\[build\]/[build]\ndistro='thisshouldfail'/" -i .cqfdrc
if ! cqfd init; then
	jtest_result pass
else
	jtest_result fail
fi

jtest_prepare "run cqfd init with an other Dockerfile should pass"
sed -e "s/thisshouldfail/centos/" -i .cqfdrc
if cqfd init; then
	jtest_result pass
else
	jtest_result fail
fi
# restore initial .cqfdrc
mv .cqfdrc.orig .cqfdrc

jtest_prepare "run cqfd init with same uid/gid dummy user should pass"
cat <<EOF >>.cqfd/docker/Dockerfile
RUN groupadd -og "${GROUPS[0]}" -f dummy && \
    useradd -s /bin/bash -ou "$UID" -g "${GROUPS[0]}" dummy
EOF
if cqfd init; then
	jtest_result pass
else
	jtest_result fail
fi

jtest_prepare "run cqfd init with --quiet should remain quiet"
if ! cqfd -q init | grep -E "^(sha256:)?[a-f0-9]{64}$"; then
	jtest_result fail
else
	jtest_result pass
fi

# restore initial Dockerfile
mv .cqfd/docker/Dockerfile.orig .cqfd/docker/Dockerfile

# restore container
if ! cqfd init; then
	jtest_log error "restore container failed"
fi
